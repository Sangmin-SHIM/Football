from setuptools import setup, find_packages  
setup(name = 'club', packages = find_packages())
setup(name = 'player', packages = find_packages())
setup(name = 'league', packages = find_packages())

# In terminal
# > python setup.py install